#include "stealth.h"
#include "msgmap.h"
#include "token.h"
#include <string>

using namespace std;

/*
int msgGetKey(string srvMsg) {
	int c, total = 0, len = srvMsg.length();

	for(c = 0; c < len; c++)
		total += (int)srvMsg[c];

	return total;
}
 */

